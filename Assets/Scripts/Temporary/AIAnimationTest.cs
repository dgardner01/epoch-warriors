using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class AIAnimationTest : MonoBehaviour
{
    //public bool animating;
    //public GameObject attack;
    //public GameObject animationStatus;
    //public GameObject progressBar;
    //public TextMeshProUGUI animationTitle;
    //private void Update()
    //{
    //    if (attack.transform.childCount > 0)
    //    {
    //        Animator animator = GetComponent<Animator>();
    //        GameObject attackInstance = attack.transform.GetChild(0).gameObject;
    //        AnimatorStateInfo animatorStateInfo = animator.GetCurrentAnimatorStateInfo(0);
    //        float progress = animatorStateInfo.normalizedTime;
    //        float length = animatorStateInfo.length;
    //        animationTitle.text = attackInstance.name.Replace("(Clone)", "").Trim();
    //        float barScale = Mathf.Lerp(0, 1, progress / length);
    //        progressBar.transform.localScale = new Vector3(barScale, 1, 1);
    //        animationStatus.SetActive(true);
    //    }
    //    else
    //    {
    //        animationStatus.SetActive(false);
    //    }
    //}
    //public void DestroyAttackInstance()
    //{
    //    Destroy(attack.transform.GetChild(0).gameObject);
    //}
}

